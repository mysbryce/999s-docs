---
title: "Handle nils"
description: "นี่คือ Script ที่จะเปลี่ยนรูปแบบการเขียนโค้ดของงานคุณไปอย่างสิ้นเชิง และทำให้มันง่ายขึ้นด้วย ns_core ของพวกเรา (Replacer of 999s-bridge)"
icon: "code"
---

สำหรับการตรวจสอบข้อมูลก่อนนำข้อมูลไปใช้งาน

## Handle nils 

<CodeGroup>
```lua client/your-client-file.lua
local var = 123
local var2 = nil

if self:handleNils(var) then
    print('var is not nil')
else
    print('var is nil')
end

if self:handleNils(var2) then
    print('var2 is not nil')
else
    print('var2 is nil')
end
```
</CodeGroup>

<Columns cols={2}>
    <div>
        ### Paramaters

        <ParamField path="value" type="any" required>
            ตัวแปรหรือค่าที่ต้องการตรวจสอบ
        </ParamField>
    </div>

    <div>
        ### Return

        `Boolean`
    </div>
</Columns>